{
  "name": "intellimarks-image-api",
  "version": "1.0.0",
  "description": "Image-only mark extraction API for IntelliMarks (student ID + mark OCR via ModelScope vision model, images resized with sharp)",
  "main": "imageapi.js",
  "scripts": {
    "start": "node imageapi.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.33.5"
  }
}
